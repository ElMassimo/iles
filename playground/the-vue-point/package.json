{
  "name": "vue-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "iles --open",
    "build": "iles build",
    "preview": "iles preview --open",
    "check": "vue-tsc --noEmit",
    "now": "npm run build && npm run preview"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "devDependencies": {
    "@iconify-json/carbon": "^1.0.7",
    "@islands/excerpt": "workspace:^0.1.0",
    "@islands/feed": "workspace:*",
    "@islands/icons": "workspace:*",
    "@islands/prism": "workspace:*",
    "iles": "workspace:*",
    "vite-plugin-windicss": "^1.5.1",
    "vue-tsc": "^0.29.8"
  }
}
