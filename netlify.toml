[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "16"

[build]
  publish = "playground/the-vue-point/dist"
  ignore = "./scripts/docs-check.sh"
  command = "npx pnpm i --store=node_modules/.pnpm-store && npx pnpm run build && cd playground/the-vue-point && npx pnpm run build"
