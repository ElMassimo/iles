/**
 * Exported sync utils should go here.
 */
export { VERSION as version, ILES_APP_ENTRY } from './constants'
export { version as viteVersion, esbuildVersion, rollupVersion, mergeConfig } from 'vite-plus'
