{
  "private": true,
  "scripts": {
    "build": "pnpm -r --parallel --filter . run build",
    "build-fast": "pnpm -r --parallel --filter . --filter !iles run build -- --no-dts && npm -C iles run build-fast",
    "dev": "pnpm -r --parallel --filter . run dev"
  }
}
